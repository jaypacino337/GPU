{
  "name": "cpu-the-game",
  "version": "1.0.0",
  "description": "CPU — an incremental idle game. Click the chip, earn cycles, build a compute empire.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
